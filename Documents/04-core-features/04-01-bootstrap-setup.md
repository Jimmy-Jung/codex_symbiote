# 핵심 기능 01: Bootstrap/Setup

> 참고: 현재 최소 템플릿에서는 setup이 기본 필수 단계가 아닙니다.

## 설명

`manifest.json`, `context.md` 같은 파일을 생성하는 setup 계열 워크플로우는 선택형 확장 기능입니다.

## 현재 기준

- 기본 템플릿은 setup 없이도 동작
- setup 파일은 특정 프로젝트 컨텍스트 저장이 필요할 때만 사용
- 기본 규칙 문서에서는 setup을 전제하지 않음
